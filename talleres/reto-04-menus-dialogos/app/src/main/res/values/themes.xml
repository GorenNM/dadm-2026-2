<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TicTacToe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TicTacToe" parent="Base.Theme.TicTacToe">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <!-- Casilla del tablero: botón delineado, sin insets ni padding para que la X/O quepa. -->
    <style name="BoardButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_width">@dimen/board_cell_size</item>
        <item name="android:layout_height">@dimen/board_cell_size</item>
        <item name="android:layout_margin">@dimen/board_cell_margin</item>
        <item name="android:padding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:textSize">@dimen/board_text_size</item>
        <item name="cornerRadius">8dp</item>
    </style>
</resources>
